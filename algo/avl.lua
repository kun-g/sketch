
function avl_init(tree)
  tree.left = nil
  tree.right = nil
  tree.nodes = 0
  tree.val = 0
end

-- insert node into tree
function avl_insert(tree, node)
  return tree
end


root = {}
avl_init(root)
